<Window x:Class="Clip.Desktop.ClipNameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="命名片段" Style="{StaticResource DialogWindow}" Width="440" SizeToContent="Height"
        ResizeMode="NoResize" WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
  <Grid x:Name="NameRoot" Background="{DynamicResource ColorNeutralBackground3}">
    <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
    <StackPanel Margin="24">
      <TextBlock Text="命名片段" Style="{StaticResource DialogTitle}" Margin="0,0,0,24" />
      <TextBlock Text="片段名称" Style="{StaticResource FieldLabel}" />
      <TextBox x:Name="NameInput" AutomationProperties.Name="片段名称" ToolTip="名称用于时间轴上的识别，不会修改源文件。" />
      <TextBlock x:Name="ValidationText" Foreground="{DynamicResource ColorDangerForeground}" Text="请输入片段名称。"
                 Visibility="Collapsed" Margin="0,8,0,0" AutomationProperties.LiveSetting="Assertive" />
    </StackPanel>
    <Border Grid.Row="1" Style="{StaticResource DialogFooter}">
      <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
        <Button Content="取消" Style="{StaticResource QuietButton}" IsCancel="True" Margin="0,0,8,0" />
        <Button x:Name="SaveNameButton" Content="保存名称" Style="{StaticResource PrimaryButton}" IsDefault="True" Click="SaveClick" />
      </StackPanel>
    </Border>
  </Grid>
</Window>
